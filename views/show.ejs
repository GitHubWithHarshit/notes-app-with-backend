<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <link rel="stylesheet" href="/stylesheets/style.css">
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body>
    <div class="main bg-zinc-900 min-h-screen px-10 py-5">
        <a class="block text-blue-400 underline" href="/">Go back to Home </a>
        <%if(filedata.length !== 0){%>
        <h1 class="inline-block mt-5 p-3 text-3xl text-slate-400 bg-zinc-800  min-w-64 rounded-lg tracking-tight text-wrap" > <%= filename.slice(0, -4)%></h1>
        <p  class="mt-3 p-3 text-slate-200 mt-5 p-3 text-xl bg-zinc-800  w-2/3 rounded-lg font-light"><%= filedata%> </p>
        <%}else{%>
            <h1 class="mt-5 text-xl text-slate-400">No details.</h1>
        <%}%>
    </div>
</body>
</html>